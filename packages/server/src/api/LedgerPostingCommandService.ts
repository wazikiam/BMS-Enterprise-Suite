// packages/server/src/api/LedgerPostingCommandService.ts
// DISABLED — Week 20
// Write-side ledger posting is intentionally unavailable.

export class LedgerPostingCommandService {
  constructor() {
    throw new Error(
      'LedgerPostingCommandService is DISABLED (Week 20)'
    );
  }
}
